package extra;

import java.net.Socket;

import br.com.ucsal.ian.client.Client;
import br.com.ucsal.ian.util.SocketConstants;

public class PrintReceivedDataFromServerThread extends Thread{

	/*
	 * private Socket socket; private Client client;
	 * 
	 * public PrintReceivedDataFromServerThread(Socket socket) { this.socket =
	 * socket; }
	 * 
	 * @Override public void run() {
	 * 
	 * this.client = new Client(socket); if(client != null) { String serverData =
	 * client.readDataFromServer(); System.out.println(serverData); }
	 * 
	 * 
	 * }
	 */

	
}
